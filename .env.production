# Frontend environment configuration

## Production (Vercel)
# Pushed to repo so the deployed site always points at the live backend.
NEXT_PUBLIC_API_URL=https://bistack-management-backend.vercel.app/api
